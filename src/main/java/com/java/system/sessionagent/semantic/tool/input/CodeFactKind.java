package com.java.system.sessionagent.semantic.tool.input;

/** Kinds indexed by Semantic's code-fact query surface. */
public enum CodeFactKind {
    TYPE,
    METHOD,
    FIELD,
    ENUM_CONSTANT,
    RECORD_COMPONENT,
    ANNOTATION_USAGE,
    TYPE_USAGE,
    API_ROUTE,
    MQ_DESTINATION,
    SCHEDULE,
    MAPPER_STATEMENT,
    MAPPER_STATEMENT_VARIANT,
    CONFIGURATION_KEY,
    ERROR_CONTRACT
}
