# Copy values into your shell environment before the live run. Do not commit local credentials.
SEMANTIC_BASE_URL=
SEMANTIC_API_TOKEN=
GOOGLE_API_KEY=
GOOGLE_GENAI_MODEL=gemini-3.1-flash-lite
SESSION_AGENT_POSTGRES_PASSWORD=
SESSION_AGENT_PORT=8090

# Reserved for a future transport integration. The current runtime does not consume them.
SLACK_APP_TOKEN=
SLACK_BOT_TOKEN=
SLACK_BOT_USER_ID=
